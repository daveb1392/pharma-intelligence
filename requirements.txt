# Supabase database client
supabase>=2.0.0

# Web scraping framework (Apify SDK is pre-installed on Apify platform)
# Note: Using specific extras instead of [all] to avoid opentelemetry-distro conflicts
crawlee[playwright,beautifulsoup,httpx]>=1.0.0

# Headless browser automation
playwright>=1.40.0

# Environment variables
python-dotenv>=1.0.0

# HTTP client (used by Crawlee)
httpx>=0.25.0

# HTML parsing (used by Crawlee)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data validation and serialization
pydantic>=2.5.0

# Logging
loguru>=0.7.0

# Optional: BigQuery sync (uncomment if needed)
# google-cloud-bigquery>=3.14.0

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.12.0
ruff>=0.1.8
